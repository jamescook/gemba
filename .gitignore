# Compiled C extension
*.bundle
*.so
*.o
*.def
*.dSYM
*.log
Makefile

# Vendor build artifacts (rebuilt with `rake deps`)
# Ignore specific build artifact dirs; vendor/rcheevos is a tracked submodule
vendor/install
vendor/mgba
vendor/install/
vendor/mgba/

# Gem build
*.gem
pkg/

# Coverage
coverage/

# Test screenshots (xvfb debug captures)
test/screenshots/

# Local dep copies for Docker builds (populated by rake docker:build)
_build_deps/

# Beads issue tracker (local DB, synced via JSONL)
.beads/

# Tool versions (developer-specific)
mise.toml

# Bundler lock (path: gems make this machine-specific)
Gemfile.lock

# YARD
.yardoc/
doc/

# OS
.DS_Store
tmp/
